---
description: Yogasana practice companion project conventions
alwaysApply: true
---

Follow `AGENTS.md` as the source of truth. This is a GitHub Pages static React app: day-aware schedule, localStorage check-offs, and optional guided timers. Keep `src/data/schedule.ts` at 2560 seconds total. Run `npm test` and `npm run build` after logic changes.
